var documenterSearchIndex = {"docs":
[{"category":"section","location":"iusac/#IUSAC","page":"IUSAC","text":"","title":"IUSAC"},{"category":"section","location":"iusac/#Background","page":"IUSAC","text":"IUSAC (Iterative Update Sample Consensus) is a repeatable variant of the RANSAC algorithm introduced by Kim et al. (2024).  Like Optimal RANSAC, it addresses the fundamental non-determinism of standard RANSAC by augmenting the hypothesis-and-verify loop with an iterative update step that steers each candidate toward the globally optimal inlier set.\n\nThe key insight of IUSAC is that once an initial hypothesis is \"close enough\" to the true model, iteratively re-estimating the model from the full current inlier set (rather than from only the minimal sample) and rescoring against all data will cause the consensus set to grow monotonically until convergence. This is backed by a formal convergence analysis in Kim et al. (2024): when the objective function is locally quadratic around the true solution (which holds for least-squares problems under mild regularity conditions), the iterative update is equivalent to one step of Newton's method and is guaranteed to decrease the residual.","title":"Background"},{"category":"section","location":"iusac/#The-algorithm","page":"IUSAC","text":"Given a dataset X of N points, a minimum sample size s, an inlier distance threshold tau_e, a stopping threshold hateta, and a convergence tolerance varepsilon, our implementation of IUSAC proceeds as follows:\n\nInitialization – outer loop — for each iteration of the outer loop, draw a random minimal sample of s points and fit a candidate model p_0.  Score it against all data to obtain the initial consensus set C_0 = operatornameinlier(X p_0 tau_e). Strategies for the initialization can vary; here we adopt a standard RANSAC approach for the initialization (see RANSAC), generating the initial hypotheses in an outer RANSAC loop with its own convergence criterion (p). The outer RANSAC loop generates increasingly likely hypotheses for input to the inner IUSAC loop, which is necessary as the convergence of IUSAC depends on an initial hypothesis \"close\" to the true model.\nIterative update – inner loop — starting from C_0, repeat:\n(a) Re-estimate the model from all current inliers: p_k = g(C_k-1).\n(b) Rescore against all data: C_k = operatornameinlier(X p_k tau_e).\n(c) Convergence check:\nIf C_k  C_k-1: set C^* = C_k-1 and stop.  The consensus set has shrunk, so the previous set was better.\nIf C_k  (1 + varepsilon)C_k-1: set C^* = C_k and stop. Growth has stagnated (relative increase below varepsilon = 0001); this generally only triggers when the number of inliers is high.\nOtherwise: continue with C_k-1 leftarrow C_k.\nStopping criterion – inner loop — if C^* geq hateta N, re-estimate p^* = g(C^*) and terminate inner loop. Otherwise, repeat steps 2–3 a number of times (keyword argument max_inner_iterations).\nStopping criterion – outer loop As our outer loop is a standard RANSAC, we use the standard RANSAC stopping criterion, where the number of outer iterations N_out is updated adaptively as\nN = fraclog(1 - p)logleft(1 - left(varepsilon_mathrminright)^sright)\nwhere varepsilon_mathrmin is the current best estimate of the inlier  fraction and s is the minimum sample size.  The algorithm also supports an  explicit early-stop threshold eta_b, which will terminate the outer loop  when varepsilon_mathrmin  varepsilon_b.","title":"The algorithm"},{"category":"section","location":"iusac/#Why-it-is-repeatable","page":"IUSAC","text":"The iterative update reliably expands any \"good enough\" initial hypothesis to the global optimal inlier set, because re-estimating from all current inliers produces a model that is better aligned to the true structure than one fitted to only s points.  As long as the RANSAC-style random sampling produces at least one outer iteration whose initial consensus set overlaps sufficiently with the true inlier set, the iterative update will guide that iteration to the optimal solution.  The probability of this happening increases with the number of outer trials, making IUSAC highly repeatable in practice.","title":"Why it is repeatable"},{"category":"section","location":"iusac/#When-to-use-IUSAC","page":"IUSAC","text":"As IUSAC is essentially RANSAC with an additional inner iteration loop,  the runtime is generally longer than standard RANSAC as it typically  makes more calls to distfn and fittingfn per data trial. However, IUSAC may converge with fewer outer RANSAC iterations, so often it is not much more expensive than standard RANSAC. However, fittingfn must support overconstrained problems which require more computation to solve than minimally-constrained problems as expected in standard RANSAC, so calls to fittingfn may be more expensive if your dataset is large. For small problems (e.g., simple line fitting) this additional cost is often negligible and IUSAC may be preferred for its robustness and repeatibility.  IUSAC may be preferrable to standard RANSAC when:\n\nrepeatability across runs (with different random seeds) is important,\nthe inlier fraction is moderate (sim 30 or more),\nfittingfn supports overconstrained fits with more than s points (required for the iterative update step).\n\nIUSAC is less suitable when:\n\nthe model fitting step does not support over-determined inputs,\nthe inlier fraction is extremely low (lesssim 5), where the outer loop will rarely produce a good initialisation,\nmultiple competing structures of similar quality are present.","title":"When to use IUSAC"},{"category":"section","location":"iusac/#API","page":"IUSAC","text":"","title":"API"},{"category":"section","location":"iusac/#Example:-fitting-a-line-in-the-presence-of-outliers","page":"IUSAC","text":"The following example generates 100 inlier points near the line y = 2x + 3, adds 100 outliers scattered over a wider region, and uses IUSAC to recover the true parameters.\n\nusing ConsensusFitting\nusing Random\nusing CairoMakie\n\nRandom.seed!(42)\n\n# ── Generate synthetic data ────────────────────────────────────────────────\na_true, b_true = 2.0, 3.0\nn_inliers  = 100\nn_outliers = 100\n\nx_in = collect(range(-10.0, 10.0; length=n_inliers))\ny_in = a_true .* x_in .+ b_true .+ randn(n_inliers)\n\nx_out = -10.0 .+ 20.0 .* rand(n_outliers)\ny_out = -25.0 .+ 50.0 .* rand(n_outliers)\n\n# Pack into a 2 × N matrix (each column is one data point [x; y])\ndata = [vcat(x_in, x_out)'; vcat(y_in, y_out)']\n\n# ── Define the fitting and distance functions ──────────────────────────────\n\n# Fit a line y = a*x + b through ≥ 2 points.\nfunction fit_line(pts)\n    n = size(pts, 2)\n    n < 2 && return []\n    if n == 2\n        # Minimally constrained: two-point exact fit\n        x1, y1 = pts[1, 1], pts[2, 1]\n        x2, y2 = pts[1, 2], pts[2, 2]\n        isapprox(x1, x2; atol=1e-10) && return []   # vertical line → degenerate\n        a = (y2 - y1) / (x2 - x1)\n        b = y1 - a * x1\n        return [a, b]\n    else\n        # Over-constrained: least-squares fit (required for iterative update)\n        A = hcat(pts[1, :], ones(n))\n        b = pts[2, :]\n        coef = A \\ b\n        return [coef[1], coef[2]]\n    end\nend\n\n# Classify points using their vertical (y-direction) residual.\nfunction line_dist(M, x, t)\n    a, b    = M[1], M[2]\n    resid   = abs.(x[2, :] .- (a .* x[1, :] .+ b))\n    inliers = findall(resid .< t)\n    return (model=M, inliers=inliers)\nend\n\n# ── Run IUSAC ──────────────────────────────────────────────────────────────\nM, inliers = iusac(data, fit_line, line_dist, 2, 2.0)\n\nprintln(\"Recovered slope:     \", round(M[1]; digits=4), \"  (true: $a_true)\")\nprintln(\"Recovered intercept: \", round(M[2]; digits=4), \"  (true: $b_true)\")\nprintln(\"Inliers identified:  \", length(inliers), \" / $(size(data, 2))\")","title":"Example: fitting a line in the presence of outliers"},{"category":"section","location":"iusac/#Visualising-the-result","page":"IUSAC","text":"outlier_mask = trues(size(data, 2))\noutlier_mask[inliers] .= false\n\nfig = Figure(size=(500, 500))\nax  = Axis(fig[1, 1];\n           xlabel=\"x\", ylabel=\"y\",\n           title=\"IUSAC line fitting\")\n\n# All data points\nscatter!(ax, data[1, outlier_mask], data[2, outlier_mask];\n         color=(:tomato, 0.7), markersize=8, label=\"Outliers\")\nscatter!(ax, data[1, inliers], data[2, inliers];\n         color=(:steelblue, 0.8), markersize=8, label=\"Inliers\")\n\n# True and recovered lines\nx_plot = range(-10.0, 10.0; length=200)\nlines!(ax, x_plot, M[1] .* x_plot .+ M[2];\n       color=:orange, linewidth=2.5, label=\"IUSAC fit\")\nlines!(ax, x_plot, a_true .* x_plot .+ b_true;\n       color=:black, linewidth=2, linestyle=:dash, label=\"True line\")\n\naxislegend(ax; position=:lt)\nfig\n\nBecause the iterative update re-estimates the model when the list of inliers is expanded, M already represents the fit to the identified inlier set and no separate \"finalizer\" step is needed. Here we will just redo the fit with the inliers to additionally estimate parameter uncertainties. You will see that the best-fit values found μ are the same as what was output in M above.\n\nusing LinearAlgebra: dot\n\n\"\"\"\n    fit_line_overconstrained(data)\n\nFit a line y = a + b*x to data given as a 2×N matrix:\n    data[1, :] = x\n    data[2, :] = y\n\n# Returns\n  - `θ` :[a, b] (slope, intercept)\n  - `Σθ`: covariance matrix of θ (2×2)\n  - `σ²`: estimated residual variance\n\"\"\"\nfunction fit_line_overconstrained(data)\n    @assert size(data, 1) == 2 \"Input must be a 2×N matrix\"\n    x = view(data, 1, :)\n    y = view(data, 2, :)\n    N = length(x)\n    @assert N ≥ 2 \"Need at least two points\"\n    A = hcat(x, ones(N))\n    θ = A \\ y\n    r = y - A * θ\n    dof = N - 2\n    @assert dof > 0 \"Need more than 2 points for covariance estimate\"\n    σ² = dot(r, r) / dof\n    Σθ = σ² * inv(A' * A)\n    return θ, Σθ, σ²\nend\n\nμ, Σθ, σ² = fit_line_overconstrained(data[:, inliers])\nprintln(\"Recovered slope:     \", round(μ[1]; digits=4), \" ± \", round(sqrt(Σθ[1,1]); digits=4), \"  (true: $a_true)\")\nprintln(\"Recovered intercept: \", round(μ[2]; digits=4), \" ± \", round(sqrt(Σθ[2,2]); digits=4), \"  (true: $b_true)\")\nprintln(\"M == μ:     \", M == μ)","title":"Visualising the result"},{"category":"section","location":"iusac/#References","page":"IUSAC","text":"This page cites the following references:\n\nKim, J.; Lee, B.; Zanetti, M. R.; Miller, K. A. and Kim, S. (2024). Iterative Update Sample Consensus (IUSAC): A repeatable algorithm for optimal consensus set. Journal of Computational and Applied Mathematics 436, 115423.\n\n\n\n","title":"References"},{"category":"function","location":"iusac/#ConsensusFitting.iusac","page":"IUSAC","text":"iusac(x, fittingfn, distfn, s, t;\n      rng = Random.default_rng(),\n      degenfn = _ -> false,\n      verbose = false,\n      verbose_io = stdout,\n      max_data_trials = 100,\n      max_trials = 1000,\n      p = 0.99,\n      eta_b = 1.0,\n      epsilon = 0.001,\n      max_inner_iterations = 100)\n\nRobustly fit a model to data using the Iterative Update Sample Consensus (IUSAC) algorithm of Kim et al. (2024).\n\nArguments\n\nx: Data array of size [...] × N (arbitrary dimensionality per data point is supported, but the last dimension must correspond to the number of data points). Commonly will be d × N, where d is the dimensionality of each data point and N is the total number of data points.\nfittingfn: Function that fits a model to a sample of data points. Must have the signature M = fittingfn(x). This function is called with subsets of varying size — from the minimal s points (outer sampling step) up to the full current inlier set (iterative update step). It must therefore implement a least-squares or otherwise over-determined fit when given more than s points. The function should return an empty collection when it cannot produce a valid model (e.g., degenerate input). It may also return a collection of multiple candidate models; in that case distfn is responsible for selecting the best one.\ndistfn: Function that scores a model against all data points and returns a NamedTuple. Must have the signature nt = distfn(M, x, t), where nt.inliers is a vector of last-dimension indices into x for which the residual is below threshold t (i.e., the inlier data points are x[:, :, ..., nt.inliers]) and nt.model is the scored model. When M holds multiple candidate models this function should select and return only the model with the most inliers.\ns: Minimum number of data points required by fittingfn to fit a model (e.g., 2 for a line, 3 for a plane, 4 for a homography).\nt: Distance threshold below which a data point is classified as an inlier.\n\nKeyword Arguments\n\nrng::Random.AbstractRNG: Random number generator to use for sampling. Defaults to Random.default_rng().\ndegenfn: Function that tests whether a candidate minimal sample would produce a degenerate model. Must have the signature r = degenfn(x) and return true when the sample is degenerate. Defaults to _ -> false, which treats every sample as non-degenerate and leaves degeneracy detection entirely to fittingfn (which should return an empty collection for degenerate inputs).\nverbose::Bool: When true, prints per-iteration diagnostics. Defaults to false.\nverbose_io::IO: IO stream to which verbose output is written. Defaults to stdout. Primarily useful for testing or redirecting output.\nmax_data_trials::Integer: Maximum number of attempts to draw a non-degenerate minimal sample before emitting a warning and advancing to the next outer iteration. Defaults to 100.\nmax_trials::Integer: Hard upper bound on the number of outer IUSAC iterations. Defaults to 1000.\np::Real: Desired probability of drawing at least one outlier-free sample, used for the same adaptive stopping criterion as RANSAC. Defaults to 0.99.\neta_b::Real: Fraction of total data points that the best consensus set must reach for the algorithm to terminate early. When |C*| ≥ eta_b * N the algorithm stops immediately and returns the current best result. Defaults to 1.0 (disabled; the algorithm always runs until the adaptive stopping criterion or max_trials is reached).\nepsilon::Real: Relative convergence tolerance for the inner iterative update loop. The inner loop terminates when the consensus set grows by less than a fraction epsilon relative to its previous size, i.e., when C_k  (1 + varepsilon)C_k-1. Defaults to 0.001.\nmax_inner_iterations::Integer: Hard upper bound on the number of inner iterative update steps performed per outer iteration. Defaults to 100.\n\nReturns\n\nM: The model with the greatest number of inliers found during the search.\ninliers: Vector of last-dimension indices of x that are inliers to M.\n\nExtended Help\n\nIUSAC augments the standard RANSAC hypothesis-and-verify loop with an inner iterative update step.  Each outer iteration proceeds as follows.\n\nInitialization (outer loop body): a minimal random sample of s points is drawn and a candidate model p_0 is fitted to it.  The initial consensus set C_0 = operatornameinlier(X p_0 tau_e) is computed by scoring the model against all data points.\n\nIterative update (inner loop): starting from C_0 and p_0, the model is repeatedly re-estimated from the entire current inlier set and rescored against all data until one of two convergence conditions is met:\n\nC_k  C_k-1 (consensus set shrank): the previous set C_k-1 is declared optimal and the inner loop terminates.\nC_k  (1 + varepsilon)C_k-1 (stagnated growth): the current set C_k is declared optimal and the inner loop terminates.\n\nWhile neither condition holds the inner loop continues, always moving toward the larger set.\n\nStopping criterion: if the best consensus set C^* found in this outer iteration satisfies C^* geq hateta N (controlled by eta_b), the algorithm terminates early.  Otherwise the outer loop continues, subject to the adaptive estimate of the number of trials required (identical to the formula used by ransac) and the hard limit max_trials.\n\nThe key insight of IUSAC is that the iterative update — re-estimating the model from the full current inlier set rather than from only s points — drives the solution toward the global optimum as long as the initial hypothesis is close enough to the true model.  This makes the algorithm notably more repeatable than standard RANSAC while adding only modest overhead per outer iteration.\n\nReferences\n\nKim et al. (2024)\n\n\n\n\n\n","title":"ConsensusFitting.iusac"},{"category":"section","location":"optimalransac/#Optimal-RANSAC","page":"Optimal RANSAC","text":"","title":"Optimal RANSAC"},{"category":"section","location":"optimalransac/#Background","page":"Optimal RANSAC","text":"Optimal RANSAC is a repeatable variant of the RANSAC algorithm introduced by Hast et al. (2013).  Standard RANSAC is inherently non-deterministic: because it selects random minimal samples, two runs on the same data can find different inlier sets, which is problematic when the algorithm is embedded in a larger pipeline that must be reproducible.  Optimal RANSAC addresses this by augmenting the standard hypothesis-and-verify loop with three iterative refinement steps that steer the search toward the global optimal inlier set, making repeated convergence to the same result much more likely than in the standard RANSAC algorithm.","title":"Background"},{"category":"section","location":"optimalransac/#The-algorithm","page":"Optimal RANSAC","text":"Optimal RANSAC shares its outer structure with standard RANSAC but, crucially, replaces the single scoring step with three tightly coupled sub-procedures whenever a candidate hypothesis yields more than a small number of tentative inliers.","title":"The algorithm"},{"category":"section","location":"optimalransac/#1.-Resample-(Algorithm-2)","page":"Optimal RANSAC","text":"A random subset of up to a quarter of the current tentative-inlier set is drawn and a new model is fitted to that subset.  The new model is then scored against the full dataset using a (optionally wider) search tolerance t_mathrmsearch.  If this produces a larger inlier set the resampling loop restarts from the expanded set; otherwise it tries up to 8 different subsets before returning.  This is directly inspired by the Local-Optimisation step of LO-RANSAC (Chum et al., 2003; Chum et al., 2004) but is triggered more aggressively: whenever a promising hypothesis is found rather than only after a global best is updated.","title":"1. Resample (Algorithm 2)"},{"category":"section","location":"optimalransac/#2.-Rescore-(Algorithm-3)","page":"Optimal RANSAC","text":"Starting from the inlier set produced by resampling, the model is iteratively re-estimated from all current inliers and scored against all data until the inlier set stops changing or after at most 20 iterations.  Using all inliers for fitting (rather than only the minimal sample s) exploits the fact that many problems admit a least-squares fit from more than s points, producing a model that is better aligned to the true structure.","title":"2. Rescore (Algorithm 3)"},{"category":"section","location":"optimalransac/#3.-Pruneset-(Algorithm-4)-—-optional","page":"Optimal RANSAC","text":"When a wider search tolerance t_mathrmsearch  t is used alongside a distfn input that returns per-point residuals, a final pruning pass removes any inlier whose residual exceeds the tight tolerance t. The most extreme inlier is removed one at a time and the model is re-estimated after each removal, so that the final model is always consistent with the retained inliers.","title":"3. Pruneset (Algorithm 4) — optional"},{"category":"section","location":"optimalransac/#Stopping-criterion","page":"Optimal RANSAC","text":"Instead of the adaptive N iterations formula used by standard RANSAC, Optimal RANSAC terminates when the same inlier-set size is re-discovered a specified number of times in succession (min_consensus, default 2).  The refinement steps make it very unlikely to find the same size by chance unless it corresponds to the true optimal set, so this simple criterion works surprisingly well in practice. This is the main convergence criterion for Optimal RANSAC; increasing min_consensus is recommended if the default value does not reliably reproduce the same inlier set and is typically most necessary when the tentative number of inliers per trial is low (this information is shown if you set the keyword argument verbose = true).","title":"Stopping criterion"},{"category":"section","location":"optimalransac/#When-to-use-Optimal-RANSAC","page":"Optimal RANSAC","text":"The runtime of Optimal RANSAC is generally longer than standard RANSAC as it typically makes more calls to distfn and fittingfn per data trial. Additionally, fittingfn must support overconstrained problems which require more computation to solve than minimally-constrained problems as expected in standard RANSAC, so calls to fittingfn may be much more expensive if your dataset is large. For small problems (e.g., simple line fitting) this additional cost is often negligible and Optimal RANSAC may be preferred for its robustness and repeatibility. Optimal RANSAC may be preferrable to standard RANSAC when:\n\nrepeatability is important regardless of RNG state (standard RANSAC is repeatible given the same seeded RNG, but generally not otherwise, while Optimal RANSAC is designed to be repeatible regardless of RNG state),\nfittingfn supports overconstrained fits with more than s points (required for the rescoring step),\nthe inlier fraction is very low (well below 5%), making  RANSAC's standard adaptive stopping criterion unreliable, or\na high-precision final inlier set is needed (using the two-tolerance mode with t_search > t).","title":"When to use Optimal RANSAC"},{"category":"section","location":"optimalransac/#API","page":"Optimal RANSAC","text":"","title":"API"},{"category":"section","location":"optimalransac/#Example:-fitting-a-line-in-the-presence-of-outliers","page":"Optimal RANSAC","text":"The following example generates 100 inlier points near the line y = 2x + 3, adds 100 outliers scattered over a wider region, and uses Optimal RANSAC to recover the true parameters.\n\nusing ConsensusFitting\nusing Random\nusing CairoMakie\n\nRandom.seed!(42)\n\n# ── Generate synthetic data ────────────────────────────────────────────────\na_true, b_true = 2.0, 3.0\nn_inliers  = 100\nn_outliers = 100\n\nx_in = collect(range(-10.0, 10.0; length=n_inliers))\ny_in = a_true .* x_in .+ b_true .+ randn(n_inliers)\n\nx_out = -10.0 .+ 20.0 .* rand(n_outliers)\ny_out = -25.0 .+ 50.0 .* rand(n_outliers)\n\n# Pack into a 2 × N matrix (each column is one data point [x; y])\ndata = [vcat(x_in, x_out)'; vcat(y_in, y_out)']\n\n# ── Define the fitting and distance functions ──────────────────────────────\n\n# Fit a line y = a*x + b\nfunction fit_line(pts)\n    n = size(pts, 2)\n    n < 2 && return []\n    if n == 2\n        # If minimally constrained,\n        x1, y1 = pts[1, 1], pts[2, 1]\n        x2, y2 = pts[1, 2], pts[2, 2]\n        isapprox(x1, x2; atol=1e-10) && return []\n        a = (y2 - y1) / (x2 - x1)\n        b = y1 - a * x1\n        return [a, b]\n    else\n        # If overconstrained, least-squares fit\n        A = hcat(pts[1, :], ones(n))\n        b = pts[2, :]\n        coef = A \\ b\n        return [coef[1], coef[2]]\n    end\nend\n\n# Classify points using their vertical (y-direction) residual.\nfunction line_dist(M, x, t)\n    a, b    = M[1], M[2]\n    resid   = abs.(x[2, :] .- (a .* x[1, :] .+ b))\n    inliers = findall(resid .< t)\n    return (model=M, inliers=inliers, residuals=resid)\nend\n\n# Run Optimal RANSAC with pruning\nM, inliers = optimalransac(data, fit_line, line_dist, 2, 2.0; t_search=4.0, min_consensus=10)\n\nprintln(\"Recovered slope:     \", round(M[1]; digits=4), \"  (true: $a_true)\")\nprintln(\"Recovered intercept: \", round(M[2]; digits=4), \"  (true: $b_true)\")\nprintln(\"Inliers identified:  \", length(inliers), \" / $(size(data, 2))\")","title":"Example: fitting a line in the presence of outliers"},{"category":"section","location":"optimalransac/#Visualising-the-result","page":"Optimal RANSAC","text":"outlier_mask = trues(size(data, 2))\noutlier_mask[inliers] .= false\n\nfig = Figure(size=(500, 500))\nax  = Axis(fig[1, 1];\n           xlabel=\"x\", ylabel=\"y\",\n           title=\"Optimal RANSAC line fitting\")\n\n# All data points\nscatter!(ax, data[1, outlier_mask], data[2, outlier_mask];\n         color=(:tomato, 0.7), markersize=8, label=\"Outliers\")\nscatter!(ax, data[1, inliers], data[2, inliers];\n         color=(:steelblue, 0.8), markersize=8, label=\"Inliers\")\n\n# True and recovered lines\nx_plot = range(-10.0, 10.0; length=200)\nlines!(ax, x_plot, M[1] .* x_plot .+ M[2];\n       color=:orange, linewidth=2.5, label=\"Optimal RANSAC fit\")\nlines!(ax, x_plot, a_true .* x_plot .+ b_true;\n       color=:black, linewidth=2, linestyle=:dash, label=\"True line\")\n\naxislegend(ax; position=:lt)\nfig\n\nGenerally a \"finalizer\" step (doing a final fit to the set of all inliers) is redundant with Optimal RANSAC, as this is already what is returned. Here we will just redo the fit with the inliers to additionally estimate parameter uncertainties. You will see that the best-fit values found μ are the same as what was output in M above.\n\nusing LinearAlgebra: dot\n\n\"\"\"\n    fit_line_overconstrained(data)\n\nFit a line y = a + b*x to data given as a 2×N matrix:\n    data[1, :] = x\n    data[2, :] = y\n\n# Returns\n  - `θ` :[a, b] (slope, intercept)\n  - `Σθ`: covariance matrix of θ (2×2)\n  - `σ²`: estimated residual variance\n\"\"\"\nfunction fit_line_overconstrained(data)\n    @assert size(data, 1) == 2 \"Input must be a 2×N matrix\"\n    x = view(data, 1, :)\n    y = view(data, 2, :)\n    N = length(x)\n    @assert N ≥ 2 \"Need at least two points\"\n    # Least-squares solution\n    A = hcat(x, ones(N)) # Design matrix: [x 1]\n    θ = A \\ y   # equivalent to (A'A)^(-1)A'y but more stable\n    r = y - A * θ # Residuals\n    # Degrees of freedom: N - number of parameters (2)\n    dof = N - 2\n    @assert dof > 0 \"Need more than 2 points for covariance estimate\"\n    # Residual variance estimate\n    σ² = dot(r, r) / dof\n    # Covariance matrix: σ² * (A'A)^(-1)\n    Σθ = σ² * inv(A' * A)\n    return θ, Σθ, σ²\nend\n\nμ, Σθ, σ² = fit_line_overconstrained(data[:, inliers])\nprintln(\"Recovered slope:     \", round(μ[1]; digits=4), \" ± \", round(sqrt(Σθ[1,1]); digits=4), \"  (true: $a_true)\")\nprintln(\"Recovered intercept:     \", round(μ[2]; digits=4), \" ± \", round(sqrt(Σθ[2,2]); digits=4), \"  (true: $b_true)\")\nprintln(\"M == μ:     \", M == μ)","title":"Visualising the result"},{"category":"section","location":"optimalransac/#References","page":"Optimal RANSAC","text":"This page cites the following references:\n\nChum, O.; Matas, J. and Kittler, J. (2003). Locally Optimized RANSAC. In: Pattern Recognition, edited by Michaelis, B. and Krell, G. (Springer Berlin Heidelberg, Berlin, Heidelberg); pp. 236–243.\n\n\n\nChum, O.; Matas, J. and Obdržálek, Š. (2004). Enhancing RANSAC by Generalized Model Optimization. In: Proceedings of the Asian Conference on Computer Vision (ACCV).\n\n\n\nHast, A.; Nysjö, J. and Marchetti, A. (2013). Optimal RANSAC – Towards a Repeatable Algorithm for Finding                the Optimal Set. Journal of WSCG 21, 21–30.\n\n\n\n","title":"References"},{"category":"function","location":"optimalransac/#ConsensusFitting.optimalransac","page":"Optimal RANSAC","text":"optimalransac(x, fittingfn, distfn, s, t;\n              rng = Random.default_rng(),\n              t_search = t,\n              degenfn = _ -> false,\n              verbose = false,\n              max_data_trials = 100,\n              max_trials = 1000,\n              min_inliers = 5,\n              min_consensus = 2)\n\nRobustly fit a model to data using the Optimal RANSAC algorithm of Hast et al. (2013), which extends standard RANSAC with three refinement steps (resample, rescore, and optionally pruneset) to produce a repeatable result.\n\nArguments\n\nx: Data array of size [...] × N (arbitrary dimensionality per data point is supported, but the last dimension must correspond to the number of data points). Commonly will be d × N.\nfittingfn: Function that fits a model to a sample of data points. Must have the signature M = fittingfn(x).  Unlike ransac, this function is called with subsets of varying size — from the minimal s points (outer sampling) up to the full current inlier set (rescore step). It must therefore implement a least-squares or otherwise over-determined fit when given more than s points.  The function should return an empty collection when it cannot produce a valid model (e.g., degenerate input). It may also return a collection of multiple candidate models; in that case distfn is responsible for selecting the best one.\ndistfn: Function that scores a model against all data points and returns a NamedTuple.  Must have the signature nt = distfn(M, x, t), where nt.inliers is a vector of last-dimension indices into x for which the residual is below threshold t and nt.model is the scored model.  When M holds multiple candidate models this function should select and return the one with the most inliers via nt.model.  To enable the optional pruning step (Algorithm 4), the returned NamedTuple must also contain a key residuals with a non-negative vector of per-point residuals (one entry per last-dimension slice of the x passed to distfn).  Pruning is only activated when both t_search > t and distfn provides nt.residuals.\ns: Minimum number of data points required by fittingfn to fit a model.\nt: Primary inlier distance threshold (used for the outer sampling step and, when pruning is enabled, as the final tight tolerance).\n\nKeyword Arguments\n\nrng::Random.AbstractRNG: Random number generator.  Defaults to Random.default_rng().\nt_search::Real: Inlier threshold used during the resampling and rescoring steps.  Must satisfy t_search ≥ t.  When t_search > t and distfn returns residuals in its NamedTuple, a pruning pass is applied after resampling to trim the result back to the tight tolerance t, yielding the highest-precision final inlier set.  Defaults to t (no separate search tolerance; pruning is skipped).\ndegenfn: Function that tests whether a minimal sample would produce a degenerate model.  Must have the signature r = degenfn(x) and return true when the sample is degenerate.  Defaults to _ -> false.\nverbose::Bool: When true, prints per-iteration diagnostics.  Defaults to false.\nverbose_io::IO: IO stream for verbose output.  Defaults to stdout.\nmax_data_trials::Integer: Maximum attempts to draw a non-degenerate minimal sample in the outer loop before emitting a warning.  Defaults to 100.\nmax_trials::Integer: Hard upper bound on outer-loop iterations.  Acts as a safety limit; the algorithm's primary stopping criterion is the min_consensus convergence condition.  Defaults to 1000.\nmin_inliers::Integer: Minimum tentative inlier count required to trigger the resampling/rescoring optimization.  Defaults to 5.\nmin_consensus::Integer: Number of times the same-size inlier set must be found before the algorithm declares convergence.  Defaults to 2, such that the algorithm must find the same inlier count twice in a row. For small inlier sets (fewer than ≈ 30 expected inliers) the paper recommends increasing this to 2 or more to avoid premature termination on a sub-optimal set.\n\nReturns\n\nM: The model with the largest (or, with pruning, the highest-quality) inlier set found by the algorithm.\ninliers: Vector of last-dimension indices of x that are inliers to M.\n\nExtended Help\n\nOptimal RANSAC augments the standard hypothesis-then-verify loop with three additional refinement steps taken whenever a hypothesis yields more than min_inliers tentative inliers.\n\nResample (Algorithm 2 of Hast et al. (2013)) draws subsets of up to a quarter of the current tentative-inlier set, fits new candidate models, and rescores them against the full dataset.  If a larger inlier set is found the loop restarts from that larger set; the procedure repeats up to 8 times per outer iteration.\n\nRescore (Algorithm 3) repeatedly re-estimates the model from the full current inlier set and rescores against all data until the inlier set stops changing or 20 iterations are exhausted.\n\nPruneset (Algorithm 4) is an optional final step enabled when t_search > t and distfn returns residuals in its NamedTuple.  It iteratively removes the point with the largest residual from the working set and re-estimates the model until every remaining point lies within the tight threshold t.\n\nThe algorithm terminates when the same inlier-set size is found min_consensus times in succession, indicating convergence to the optimal set.  Unlike standard RANSAC, the stopping criterion does not depend on a statistical threshold over the inlier fraction; this makes the algorithm applicable to very highly contaminated sets (inlier ratios well below 5%).\n\nRepeatability\n\nThe strong local-refinement steps (resample + rescore) reliably guide the search toward the global maximum from almost any starting hypothesis, making Optimal RANSAC near-deterministic across runs even with different random seeds, provided the data has a single dominant structure.  Pass the same seeded rng to obtain bit-for-bit identical results.\n\nLimitations\n\nThe algorithm is only appropriate when the model can be fitted to more than the minimal s points — the fittingfn must accept over-determined inputs. For problems with multiple competing structures of similar quality the algorithm may converge to a locally optimal set rather than the global optimum.\n\nReferences\n\nHast et al. (2013)\n\n\n\n\n\n","title":"ConsensusFitting.optimalransac"},{"category":"section","location":"#ConsensusFitting","page":"Home","text":"This package implements robust fitting algorithms to find best-fit models in the presence of outliers. Documentation for each algorithm can be found in separate pages in the sidebar.\n\n","title":"ConsensusFitting"},{"category":"section","location":"refs/#bibliography","page":"Bibliography","text":"Chum, O.; Matas, J. and Kittler, J. (2003). Locally Optimized RANSAC. In: Pattern Recognition, edited by Michaelis, B. and Krell, G. (Springer Berlin Heidelberg, Berlin, Heidelberg); pp. 236–243.\n\n\n\nChum, O.; Matas, J. and Obdržálek, Š. (2004). Enhancing RANSAC by Generalized Model Optimization. In: Proceedings of the Asian Conference on Computer Vision (ACCV).\n\n\n\nFischler, M. A. and Bolles, R. C. (1981). Random Sample Consensus: A Paradigm for Model Fitting with                Applications to Image Analysis and Automated Cartography. Communications of the ACM 24, 381–395.\n\n\n\nHartley, R. and Zisserman, A. (2004). Multiple View Geometry in Computer Vision. 2 Edition (Cambridge University Press, Cambridge).\n\n\n\nHast, A.; Nysjö, J. and Marchetti, A. (2013). Optimal RANSAC – Towards a Repeatable Algorithm for Finding                the Optimal Set. Journal of WSCG 21, 21–30.\n\n\n\nKim, J.; Lee, B.; Zanetti, M. R.; Miller, K. A. and Kim, S. (2024). Iterative Update Sample Consensus (IUSAC): A repeatable algorithm for optimal consensus set. Journal of Computational and Applied Mathematics 436, 115423.\n\n\n\n","title":"Bibliography"},{"category":"section","location":"ransac/#RANSAC","page":"RANSAC","text":"","title":"RANSAC"},{"category":"section","location":"ransac/#Background","page":"RANSAC","text":"RANSAC (Random Sample Consensus) is a robust model-fitting algorithm introduced by (Fischler and Bolles, 1981).  Unlike ordinary least-squares methods, which are sensitive to outliers, RANSAC explicitly divides the data into inliers (points well explained by the model) and outliers (points that are not), and seeks the model that maximises the size of the inlier set.  It has become a foundational tool in computer vision, photogrammetry, and geometric estimation; a thorough treatment can be found in (Hartley and Zisserman, 2004).","title":"Background"},{"category":"section","location":"ransac/#The-algorithm","page":"RANSAC","text":"Given a dataset of N points, the minimum number of points s needed to uniquely determine a model (e.g. s = 2 for a line, s = 3 for a plane), and an inlier distance threshold t, one iteration of RANSAC proceeds as follows:\n\nHypothesis — draw a random minimal sample of s points and fit a candidate model to them.\nVerification — evaluate every data point against the candidate model; collect those within distance t as its consensus set (inlier set).\nUpdate — if the consensus set is larger than the current best, record the candidate model and its inlier set.\n\nThe number of iterations is chosen adaptively so that, with probability p (typically 0.99), at least one drawn sample is free from outliers.  If the current best inlier fraction is varepsilon, the expected number of trials required is\n\nN = fraclog(1 - p)logleft(1 - varepsilon^sright)\n\nRANSAC updates this estimate after each improvement, allowing it to terminate early when a satisfying model is found quickly.","title":"The algorithm"},{"category":"section","location":"ransac/#When-to-use-RANSAC","page":"RANSAC","text":"RANSAC is well suited to problems where\n\nthe fraction of outliers is large (up to 50% or more),\nthe inlier noise is small relative to the outlier spread, and\nthe fitting step for the minimal sample is fast.\n\nIt is less suitable when the minimum sample size s is large (the required number of trials grows exponentially with s) or when computing the full-dataset residuals is expensive.","title":"When to use RANSAC"},{"category":"section","location":"ransac/#API","page":"RANSAC","text":"","title":"API"},{"category":"section","location":"ransac/#Example:-fitting-a-line-in-the-presence-of-outliers","page":"RANSAC","text":"The following example generates 100 inlier points near the line y = 2x + 3, adds 100 outliers scattered over a wider region, and uses RANSAC to recover the true parameters.\n\nusing ConsensusFitting\nusing Random\nusing CairoMakie\n\nRandom.seed!(42)\n\n# ── Generate synthetic data ────────────────────────────────────────────────\na_true, b_true = 2.0, 3.0\nn_inliers  = 100\nn_outliers = 100\n\nx_in = collect(range(-10.0, 10.0; length=n_inliers))\ny_in = a_true .* x_in .+ b_true .+ randn(n_inliers)\n\nx_out = -10.0 .+ 20.0 .* rand(n_outliers)\ny_out = -25.0 .+ 50.0 .* rand(n_outliers)\n\n# Pack into a 2 × N matrix (each column is one data point [x; y])\ndata = [vcat(x_in, x_out)'; vcat(y_in, y_out)']\n\n# ── Define the fitting and distance functions ──────────────────────────────\n\n# Fit a line y = a*x + b through exactly 2 points.\nfunction fit_line(pts)\n    x1, y1 = pts[1, 1], pts[2, 1]\n    x2, y2 = pts[1, 2], pts[2, 2]\n    isapprox(x1, x2; atol=1e-10) && return []   # vertical line → degenerate\n    a = (y2 - y1) / (x2 - x1)\n    b = y1 - a * x1\n    return [a, b]\nend\n\n# Classify points using their vertical (y-direction) residual.\nfunction line_dist(M, x, t)\n    a, b    = M[1], M[2]\n    resid   = abs.(x[2, :] .- (a .* x[1, :] .+ b))\n    inliers = findall(resid .< t)\n    return (model=M, inliers=inliers)\nend\n\n# ── Run RANSAC ─────────────────────────────────────────────────────────────\nM, inliers = ransac(data, fit_line, line_dist, 2, 2.0)\n\nprintln(\"Recovered slope:     \", round(M[1]; digits=4), \"  (true: $a_true)\")\nprintln(\"Recovered intercept: \", round(M[2]; digits=4), \"  (true: $b_true)\")\nprintln(\"Inliers identified:  \", length(inliers), \" / $(size(data, 2))\")","title":"Example: fitting a line in the presence of outliers"},{"category":"section","location":"ransac/#Visualising-the-result","page":"RANSAC","text":"outlier_mask = trues(size(data, 2))\noutlier_mask[inliers] .= false\n\nfig = Figure(size=(500, 500))\nax  = Axis(fig[1, 1];\n           xlabel=\"x\", ylabel=\"y\",\n           title=\"RANSAC line fitting\")\n\n# All data points\nscatter!(ax, data[1, outlier_mask], data[2, outlier_mask];\n         color=(:tomato, 0.7), markersize=8, label=\"Outliers\")\nscatter!(ax, data[1, inliers], data[2, inliers];\n         color=(:steelblue, 0.8), markersize=8, label=\"Inliers\")\n\n# True and recovered lines\nx_plot = range(-10.0, 10.0; length=200)\nlines!(ax, x_plot, M[1] .* x_plot .+ M[2];\n       color=:orange, linewidth=2.5, label=\"RANSAC fit\")\nlines!(ax, x_plot, a_true .* x_plot .+ b_true;\n       color=:black, linewidth=2, linestyle=:dash, label=\"True line\")\n\naxislegend(ax; position=:lt)\nfig\n\nOnce the inlier set has been identified, a final fit can be performed to the set of all inliers if desired.\n\nusing LinearAlgebra: dot\n\n\"\"\"\n    fit_line_overconstrained(data)\n\nFit a line y = a + b*x to data given as a 2×N matrix:\n    data[1, :] = x\n    data[2, :] = y\n\n# Returns\n  - `θ` :[a, b] (slope, intercept)\n  - `Σθ`: covariance matrix of θ (2×2)\n  - `σ²`: estimated residual variance\n\"\"\"\nfunction fit_line_overconstrained(data)\n    @assert size(data, 1) == 2 \"Input must be a 2×N matrix\"\n    x = view(data, 1, :)\n    y = view(data, 2, :)\n    N = length(x)\n    @assert N ≥ 2 \"Need at least two points\"\n    # Least-squares solution\n    A = hcat(x, ones(N)) # Design matrix: [x 1]\n    θ = A \\ y   # equivalent to (A'A)^(-1)A'y but more stable\n    r = y - A * θ # Residuals\n    # Degrees of freedom: N - number of parameters (2)\n    dof = N - 2\n    @assert dof > 0 \"Need more than 2 points for covariance estimate\"\n    # Residual variance estimate\n    σ² = dot(r, r) / dof\n    # Covariance matrix: σ² * (A'A)^(-1)\n    Σθ = σ² * inv(A' * A)\n    return θ, Σθ, σ²\nend\n\nμ, Σθ, σ² = fit_line_overconstrained(data[:, inliers])\nprintln(\"Recovered slope:     \", round(μ[1]; digits=4), \" ± \", round(sqrt(Σθ[1,1]); digits=4), \"  (true: $a_true)\")\nprintln(\"Recovered intercept:     \", round(μ[2]; digits=4), \" ± \", round(sqrt(Σθ[2,2]); digits=4), \"  (true: $b_true)\")","title":"Visualising the result"},{"category":"section","location":"ransac/#References","page":"RANSAC","text":"This page cites the following references:\n\nFischler, M. A. and Bolles, R. C. (1981). Random Sample Consensus: A Paradigm for Model Fitting with                Applications to Image Analysis and Automated Cartography. Communications of the ACM 24, 381–395.\n\n\n\nHartley, R. and Zisserman, A. (2004). Multiple View Geometry in Computer Vision. 2 Edition (Cambridge University Press, Cambridge).\n\n\n\n","title":"References"},{"category":"function","location":"ransac/#ConsensusFitting.ransac","page":"RANSAC","text":"ransac(x, fittingfn, distfn, s, t;\n       rng = Random.default_rng(),\n       degenfn = _ -> false, \n       verbose = false,\n       max_data_trials = 100, \n       max_trials = 1000, \n       p = 0.99)\n\nRobustly fit a model to data using the RANSAC (Random Sample Consensus) algorithm.\n\nArguments\n\nx: Data array of size [...] × N (arbitrary dimensionality per  data point is supported, but the last dimension must correspond  to the number of data points). Commonly will be d x N, where  d is the dimensionality of each data point and N is the total  number of data points.\nfittingfn: Function that fits a model to a minimal sample of s data points. Must have the signature M = fittingfn(x).  The function should return an empty collection when it cannot produce a valid model (e.g., degenerate input).  It may also return a collection of multiple candidate models (for example, up to three fundamental matrices can be recovered from seven point correspondences); in that case distfn is responsible for selecting the best one.\ndistfn: Function that scores a model against all data points and returns a NamedTuple.  Must have the signature nt = distfn(M, x, t), where nt.inliers is a vector of last-dimension indices into x for which the residual is below threshold t (i.e., the inlier data points are x[:, :, ..., nt.inliers]) and nt.model is the scored model.  When M holds multiple candidate models this function should select and return only the model with the most inliers.\ns: Minimum number of data points required by fittingfn to fit a model (e.g., 2 for a line, 3 for a plane, 4 for a homography).\nt: Distance threshold below which a data point is classified as an inlier.\n\nKeyword Arguments\n\nrng::Random.AbstractRNG: Random number generator to use for sampling.  Defaults to Random.default_rng().\ndegenfn: Function that tests whether a candidate minimal sample would produce a degenerate model.  Must have the signature r = degenfn(x) and return true when the sample is degenerate.  Defaults to _ -> false, which treats every sample as non-degenerate and leaves degeneracy detection entirely to fittingfn (which should return an empty collection for degenerate inputs).\nverbose::Bool: When true, prints the current trial number and the adaptive estimate of the total number of trials required at each iteration.  Defaults to false.\nverbose_io::IO: IO stream to which verbose output is written.  Defaults to stdout.  Primarily useful for testing or redirecting output.\nmax_data_trials::Integer: Maximum number of attempts to draw a non-degenerate minimal sample before emitting a warning and advancing to the next outer iteration.  Defaults to 100.\nmax_trials::Integer: Hard upper bound on the number of RANSAC iterations. Defaults to 1000.\np::Real: Desired probability of drawing at least one outlier-free sample. Controls the adaptive stopping criterion.  Defaults to 0.99.\n\nReturns\n\nM: The model with the greatest number of inliers found during the search.\ninliers: Vector of column indices of x that are inliers to M.\n\nExtended Help\n\nRANSAC alternates between two steps.  In the hypothesis step a minimal random sample of s points is drawn and a candidate model is fitted to it. In the verification step every data point is tested against the candidate model; those within distance t form the consensus set (inlier set) of that model.  The hypothesis with the largest consensus set is retained. The algorithm terminates adaptively once the expected number of trials required to find an outlier-free sample with probability p has been reached, or after max_trials iterations, whichever comes first.\n\nThe adaptive estimate used for the number of trials is\n\nN = fraclog(1 - p)logleft(1 - varepsilon^sright)\n\nwhere varepsilon is the current best estimate of the inlier fraction and s is the minimum sample size.\n\nReferences\n\nFischler and Bolles (1981)\nHartley and Zisserman (2004)\n\n\n\n\n\n","title":"ConsensusFitting.ransac"}]
}
